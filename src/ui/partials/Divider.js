import React from 'react'
import { View } from 'react-native'

const Divider = () => {
    return (
        <View
            style={{
                height: 0.3,
                width: '90%',
                backgroundColor: '#080808',
            }}
        />
    );
}

export default Divider